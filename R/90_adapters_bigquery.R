# Optional adapter stub (no runtime dependency).
# This file intentionally avoids requiring any cloud packages.
# Implementations should be added only when explicitly requested.

bq_adapter_available <- function() {
  FALSE
}
